https://codepen.io/maydin/pen/vYqXRaQ






useState, useReducer

import {
  ChevronUp,
  ChevronsUp,
  ChevronDown,
  ChevronsDown,
  RotateCcw,
  Hash
} from "https://esm.sh/react-feather";

onClick


clamp(Math.ceil(Math.random() * 100), 1, 100)

const clamp = (val, min = 0, max = 1) => {
  if (min > max) {
    [min, max] = [max, min];
  }

  return Math.max(min, Math.min(max, val));
};





