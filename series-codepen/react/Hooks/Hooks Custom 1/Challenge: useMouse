https://codepen.io/maydin/pen/ByBWzRW

fare hareketinden koordinat

useState, useEffect, useRef


document.addEventListener("mousemove", handleMouseMove);
document.removeEventListener("mousemove", handleMouseMove);

x: e.clientX,
          y: e.clientY,
          elementX: element ? Math.round(e.clientX - elementPosition.left) : 0,
          elementY: element ? Math.round(e.clientY - elementPosition.top) : 0,
          elementPositionX: elementPosition.left,
          elementPositionY: elementPosition.top

