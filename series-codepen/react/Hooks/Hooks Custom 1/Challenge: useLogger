https://codepen.io/maydin/pen/gbYRGdQ

useState, 
useEffect, 
useRef

function useLogger(...arg)

["First", "Second", "Third"].map((item, index) => {
          const isActive = count % 3 === index;
          return (
            <FirstChild
              key={index}
              name={item}
              isActive={isActive}
              count={count}
            />
          );
        })


